## Network used:

```
LSTMNetwork (
  (embedding1): Embedding(18, 64)
  (embedding2): Embedding(58, 64)
  (lstm): LSTM(128, 64, num_layers=2, batch_first=True)
  (fc): Linear (64 -> 59)
  (softmax): LogSoftmax ()
)
```
